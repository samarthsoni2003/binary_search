int singleNonDuplicate(vector<int>& arr)
{
	int i=0;
	int n=arr.size();
	while(i<n){
		if(i==n-1){
			return arr[i];
		}
		if(arr[i]==arr[i+1]){
			i+=2;
		}
		else{
			return arr[i];
		}
	}      // question solved but tc=0(n)
}

